SELECT 
        'Successful' AS 'Installation',
        product_id,
        product_name,
        product_price,
        category_id
    FROM
        product;

        select 'Successful' AS 'Installation', product_id, product_name, product_price, category_id from product;